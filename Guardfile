# classes
guard 'coffeescript', :input => 'coffeescripts', :output => 'javascripts'

# specs
guard 'coffeescript', :input => 'spec', :output => 'spec/compiled'


guard 'livereload' do
	watch('spec')
	watch('javascripts')
end
